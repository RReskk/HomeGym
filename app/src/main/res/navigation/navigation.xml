<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/plan">

    <fragment
        android:id="@+id/plan"
        android:name="com.example.homegym.Plan"
        android:label="fragment_plan"
        tools:layout="@layout/fragment_plan" />
    <fragment
        android:id="@+id/lessons"
        android:name="com.example.homegym.Lessons"
        android:label="fragment_lessons"
        tools:layout="@layout/fragment_lessons" />
    <fragment
        android:id="@+id/reports"
        android:name="com.example.homegym.Reports"
        android:label="fragment_reports"
        tools:layout="@layout/fragment_reports" />
    <fragment
        android:id="@+id/profile"
        android:name="com.example.homegym.Profile"
        android:label="fragment_profile"
        tools:layout="@layout/fragment_profile" />
</navigation>